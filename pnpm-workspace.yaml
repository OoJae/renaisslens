packages:
  - apps/*
  - packages/*

# pnpm >=10 blocks dependency lifecycle scripts by default.
# Build approvals are committed here so CI and fresh clones never
# need an interactive prompt.
allowBuilds:
  better-sqlite3: true
  esbuild: true
  playwright: true
  sharp: true

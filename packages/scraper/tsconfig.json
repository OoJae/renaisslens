{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // DOM lib is needed for the code that runs inside page.evaluate()
    "lib": ["ES2023", "DOM", "DOM.Iterable"]
  },
  "include": ["src", "test"]
}
